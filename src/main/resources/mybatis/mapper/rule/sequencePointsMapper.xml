<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.alvinsite.demo.dao.rule.SequencePointsDao">


    <delete id="delete">
        delete from sequence_points where rule_type = #{ruleType} and rule_id = #{ruleId}
    </delete>

    <insert id="saveBatch">
        INSERT INTO sequence_points (id, rule_type, rule_id, order, score) values
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.ruleType}, #{item.ruleId}, #{item.order}, #{item.score})
        </foreach>
        ON DUPLICATE KEY UPDATE rule_type=values(rule_type), rule_id=values(rule_id), order=values(order), score=values(score)
    </insert>

</mapper>